You are a senior developper in front end backend, your role is to help me create an AI reply agent. The tech stack is : streamlit, supabase, instantly, grok_api_key (free tier).  using our GROQ_API_KEY= we are going to build the following module. It will be a streamlit module. Your role will be to reorganise into a professional modular tool what im going to present. Here is what i want. All campaign have a webhook : when receiving a response in instantly send the info to our code base. THis is different from the webhook "when a lead is newly marked interested". Here we aim specifically for people who received the first or second or third reply and shoot us a text about something. When receiving this, our codebase effectuate the following workflow : store the response in db, use grok API key to read the text and craft a response based on the ~/dev/hercule.dev/app/cvg CVG and following a prompt we have created for it. The response is short ideally max 3-6 sentences. It never starts with a generic "Bonjour" but more with "Pour répondre à votre question" ou bien "Voici plus d'explications." The email is plain text, signed Béatrice Meyer and when possible add some urgency att the CTA to invite for the booking again. The structure of the reply is : aknowledge, adresse, redirect (CTA). Tell me if such a tool is possible. This tool will be again multy tenancy meaning we use the same API key for every campaign, the same script, the same CVG .md file reference because it will both contains the info for agence & entreprise. When not sure about a reply because their isn't any info about the subject, the prompt emphasise on not responding. Instead we have a dedicated tab inside this tool called "Problem" with all the replies that need to be treated. We call this tool the AI sales agent. Inside our tool streamlite their is an onboarding flow to initiate this AI for any campaign. First we select the campaign. Then we select the niche config of the campaign so the AI know who it is talking to (un grossiste ? un comptable ? une agence web) all the config available are inside : ~/dev/hercule.dev/app/streamlit_scraper. Then we select : buyer / seller. Il y a un petit pop qui explique la diff : buyer sont ceux qui candidate pour avoir des demandes (ici des agence). seller sont les entreprise qui recherche une agence. Une fois que tout a été sélectionné, le CLI est capable de nous montrer le bon prompt adapté à l'IA (car tout les prompt auront déjà été registered au préalable, on ne les crée pas sur le spot. when creating a new streamlit boostrap inside : ~/dev/hercule.dev/app/streamlit_scraper now we also create the prompt associated with this niche). Then we read the prompt and validate. Then we click on initiate the webhook. Then we click on send to prod. It loads. Then ask us to refresh. Then when we select the campaign again, everything is set and it tells us normally Inbox : "waiting for replies". When a reply is captured we see it inside the inbox, we can read the convo history and see what the AI said. In the second tab (problem) is just a filter of the same inbox but with prospect the AI didn't managed to answer to. We can manually reply. All the rules about the timezone possible of response apply when responding manually. However the AI can respond at any time possible. Its immediate. Because we've said so much and the memory is limited we are going to first document, consolidate, improve our module tool inside a documentation inside : ~/dev/hercule.dev/doc/tech-stack/tool/streamlit_reply_agent. 